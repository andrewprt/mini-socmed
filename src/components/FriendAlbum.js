import React from 'react';

const Friendalbum = ({ title }) => {
    return (
        <div>
            <div>
                <h2>{title}</h2>
            </div>
        </div>
    );
}

export default Friendalbum;
